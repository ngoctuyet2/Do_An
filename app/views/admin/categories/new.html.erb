<%= form_with(scope: :category, url: admin_create_categories_path,local: true) do |form| %>
   <div class="form-group">
     <%= form.label :name %>
     <%= form.text_field :name, id: :category_name, class:'form-control'%>
   </div>
     <div class="actions">
     <%= form.submit %>
   </div>
 <% end %>
