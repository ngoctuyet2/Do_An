<%= form_with(scope: :category, url: admin_update_categories_path,method: :put,local: true) do |form| %>
   <div class="form-group">
     <%= form.label :name %>
     <%= form.text_field :name, id: :category_name, class:'form-control'%>
   </div>
     <div class="actions">
     <%= form.submit %>
   </div>
 <% end %>
